1. git init
2. git add . (filename.extension)
3. git commit -m "message"
4. git remote add origin <remote-url>
	Example:
		git remote add origin  https://github.com/venkataDeepthiSri/VI-sem_jfs.git
5. git push origin main
	master-old
	main-new
6. you can check in remote repository
--------------------------------------------------------------------------------------------------------------------------------
if you can add any files in local repository or you can change the existing files in local repository
1. git status (verification)
2. git add . 
3. git commit -m "message"
4. git push origin master
------------------------------------------------------------------------------------------------------------------------------
To remove git - rmdir /s /q .git